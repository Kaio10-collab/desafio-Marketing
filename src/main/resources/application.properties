## s�o configura��es de acesso
spring.datasource.url=jdbc:mysql:${URL_LOCAL}
spring.datasource.username=${USER_BD}
spring.datasource.password=${PASSWORD_BD}

## configura��o de comportamento
spring.jpa.hibernate.ddl-auto=update

#Config JWT
jwt.segredo=${JWT_SEGREDO}
jwt.time=${TIME_TEMPO}